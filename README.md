# BERT

This repository contains the code for keyphrase extraction using BERT (Bidirectional Encoder Representations from Transformers). It also contains the text files which was used to test BERT and SciBERT on.
